
- Handlers
  - Handlers might need more than just the keepers, like the stores, check what SDK handlers look like

- Document better the new behavior of skip_halt_height (there's a related flag as well right?)

- store versinning ?
  - called Upgrade.StateFOrmatVersion
	- seems like yes, it is returned by LoadLatestVersion (no its a different thing, why is this func mentioned actually?)
	- how do we introduce it in the store for the next upgrade (bc it won't be there)
	- what is the check about versionning in case of nil upgrade handler: does this store version==min_halt_version
	- resolve alternative about using version instead of StateFormatAfter
	- rename StateFormatAfter to something more meaningful ? (use Store instead of State)?

- investigate realm patching with realm store migration (see open question 1)

- investigate about WillSetParams that should reject a value the node cannot parse

- investigate rollbacks (theres a appd rollback command)

- check if source line numbers are still accured 

