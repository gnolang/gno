package authz

/*
import "std"

type AccessControl interface {
	// ACLCan is prefixed with ACL to allow implementing structure to reserve
	// the Can keyword for a potentially different function signature, i.e.,
	// without resourceAndContext, or with a specific type instead of any.
	ACLCan(addr std.Address, action string, resourceAndContext any) bool
}
*/
