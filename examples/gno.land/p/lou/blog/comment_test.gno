package blog

// Post renders a single comment.

// Post renders multiple comments in order.

// Nested replies to comments render in thread format.

// Comment includes correct author resolution.

// Comment with invalid author still renders gracefully (e.g. fallback name).

// Replies can be added to comments and are nested correctly.
