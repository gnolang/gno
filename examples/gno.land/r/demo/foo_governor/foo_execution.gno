package foo_governor

func Execute(proposalId string) {
	// do the actual execution work here
	// list path
	// check path address == proposal address or check path equivalance?
	gvr.Execute(proposalId)
}
