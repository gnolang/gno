package commondao

func Render(string) string {
	// TODO: Implement Render support
	// should do:
	//  pager = mustGetByXXX
	// for _, dao := range pager.Items {
	// render daos by pagination, by some index (ie newest created)
	// then call  p/commondao.Render(), etc
	// }
	// }

	return ""
}
