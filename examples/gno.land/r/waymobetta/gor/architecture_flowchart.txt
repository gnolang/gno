```mermaid
flowchart TB
    A[New PR Submission] --> B(CI Triggers)
    B --> C(Script collects raw PR data, preps and sends to Evaluation server)
    C --> D[(Runs AI models, Generates evaluation)]
    D --> E(Posts data to Realm)
    F[Evaluation server posts results back to Github via PR ID]
    E --- F
```
