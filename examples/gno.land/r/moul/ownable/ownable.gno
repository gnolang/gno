package ownable

// – Ownavoe, members, dao implements the CannDoEr
// Recursive usage of it so a member list is owned by a single admin( but the admin can give ownership to member list
//  Gnosis safe style using this with threshold
// : affMember + update threshold at the same time
// Composition!!!! OrCanDo(à,b) admin + dao
