// Package validators implements the on-chain validator set management through Proof of Authority.
// The Realm exposes only a public executor for govdao proposals, that can suggest validator set changes.
// Validator set changes are propagated out through the VM params keeper.
package validators
