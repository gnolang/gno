# Run gno build in a dir without go files

! gno build ...

! stdout .+
stderr -count=2 'dir .*: no matching files'

-- go.mod --
module gnobuild

-- main.gno --
package main

func main() {}

-- sub/sub.gno --
package sub
