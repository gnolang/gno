# testing gno lint command: not declared error

! gno lint .

cmp stdout stdout.golden
cmp stderr stderr.golden

-- bad_file.gno --
package hello

func Hello() {
	bar.Foo()
	println("Hello", 42)
}

-- gnomod.toml --
module = "gno.land/p/demo/hello"

-- stdout.golden --
-- stderr.golden --
bad_file.gno:4:2: undefined: bar (code=gnoTypeCheckError)
