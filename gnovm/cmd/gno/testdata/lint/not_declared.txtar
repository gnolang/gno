# testing gno lint command: not declared error

! gno lint .

cmp stdout stdout.golden
cmp stderr stderr.golden

-- bad_file.gno --
package main

func main() {
	hello.Foo()
	println("Hello", 42)
}

-- gnomod.toml --
module = "gno.land/p/demo/hello"

-- stdout.golden --
-- stderr.golden --
bad_file.gno:4:2: error: undefined: hello (gnoTypeCheckError)

Found 1 issue(s): 1 error(s), 0 warning(s), 0 info
