# testing gno lint command: bad import error

! gno lint .

cmp stdout stdout.golden
cmp stderr stderr.golden

-- bad_file.gno --
package main

import "python"

func main() {
	println("Hello", 42)
}

-- gnomod.toml --
module = "bad_import"
gno = "0.9"

-- stdout.golden --
-- stderr.golden --
bad_file.gno:3:8: error: unknown import path "python" (gnoImportError)

Found 1 issue(s): 1 error(s), 0 warning(s), 0 info
