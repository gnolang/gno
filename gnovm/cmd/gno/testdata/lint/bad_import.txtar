# testing gno tool lint command: bad import error

! gno tool lint ./bad_file.gno

cmp stdout stdout.golden
stderr 'bad_file\.gno:3:8: package python is not in std \(.+\) \(code=5\)'

-- bad_file.gno --
package main

import "python"

func main() {
	println("Hello", 42)
}

-- gno.mod --
module gno.land/p/test

-- stdout.golden --