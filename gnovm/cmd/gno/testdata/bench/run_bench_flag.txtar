# Test gno run --benchops flag

gno run --benchops main.gno

stdout 'Hello'
stderr '--- Benchops Profile ---'
stderr 'Profiling Results'
stderr 'Opcode Statistics'

-- main.gno --
package main

func main() {
	println("Hello")
}
