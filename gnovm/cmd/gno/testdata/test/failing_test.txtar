# Test with a failing _test.gno file

! gno test -v .


stdout '=== RUN   TestAlwaysFailing'
stdout '--- FAIL: TestAlwaysFailing'
stderr 'FAIL: 0 build errors, 1 test errors'

-- failing.gno --
package failing

-- failing_test.gno --
package failing

import "testing"

func TestAlwaysFailing(t *testing.T) {
	t.Fatal("failed")
}
