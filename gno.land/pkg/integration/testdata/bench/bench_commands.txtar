# Test bench command basic functionality
#
# This test verifies the bench start/stop commands work correctly
# and generate valid JSON profile output.
#
# Profile outputs are automatically written to this file when
# running with UPDATE_SCRIPTS=true

gnoland start

# Test basic bench start/stop cycle
bench start first.json
bench stop

# Test multiple bench cycles
bench start second.json
bench stop
-- first.json --
-- second.json --
