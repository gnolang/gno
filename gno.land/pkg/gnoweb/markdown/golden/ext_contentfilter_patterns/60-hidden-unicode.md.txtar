-- input.md --
Text with hidden​​​ zero width characters inside.

Standard text encoding and format.
-- output.html --
<p>Text with hidden[hidden] zero width characters inside.</p>
<p>Standard text encoding and format.</p>
