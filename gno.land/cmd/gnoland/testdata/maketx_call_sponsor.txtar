# test for maketx call with sponsorship
loadpkg gno.land/r/foo/bar $WORK

# start a new node
gnoland start

## execute GetCaller
gnokey maketx call -pkgpath gno.land/r/foo/bar -func GetCaller -gas-fee 1000000ugnot -gas-wanted 2000000 --sponsor=${USER_ADDR_test1} -broadcast -chainid=tendermint_test test1

## compare GetCaller
stdout OK!
stdout 'GAS WANTED: 2000000'
stdout 'GAS USED:   \d+'
stdout 'HEIGHT:     \d+'
stdout 'EVENTS:     \[\]'

-- realm.gno --
package call_realm

import (
    "std"
)

func GetCaller() string {
 	return std.PrevRealm().Addr().String()
}