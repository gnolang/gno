# Smoke test: verify the node is running and gnokey can connect

# Wait for genesis to finish processing
sleep 2s

# Query the test user's account
gnokey query auth/accounts/$USER_ADDR
stdout 'coins'

-- cluster --
validators: 1
