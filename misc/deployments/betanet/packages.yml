# Betanet genesis: realms to include.
# Dependencies (both p/ and r/) are auto-resolved from source imports.
# Each entry is a directory under examples/gno.land/ — all packages
# within are included recursively.

realms:
  - r/sys
  - r/gov
  - r/gnoland/blog
  - r/gnoland/wugnot
  - r/gnoland/coins
  - r/gnoland/boards2
