name: gnovm

on:
  push:
    branches:
      - master
  workflow_dispatch:
  pull_request: 
    paths:
      - "gnovm/**"
      - ".github/**"

jobs:
  main:
    name: Run Main
    uses: ./.github/workflows/templates/main.yml
    with:
      modulepath: "gnovm"