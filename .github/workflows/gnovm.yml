name: gnovm

on:
  push:
    branches:
      - master
  workflow_dispatch:
  pull_request: 
    paths:
      - "gnovm/**"
      - ".github/**"

jobs:
  main:
    name: Run Main
    uses: ./.github/workflows/main_template.yml
    with:
      modulepath: "gnovm"