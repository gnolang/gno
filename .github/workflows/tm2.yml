name: tm2

on:
  push:
    branches:
      - master
  workflow_dispatch:
  pull_request: 
    paths:
      - "tm2/**"
      - ".github/**"

jobs:
  main:
    name: Run Main
    uses: ./.github/workflows/templates/main.yml
    with:
      modulepath: "tm2"