name: gnomd

on:
  push:
    branches:
      - master
  workflow_dispatch:
  pull_request: 
    paths:
      - "contribs/gnomd/**"
      - ".github/**"

jobs:
  main:
    name: Run Main
    uses: ./.github/workflows/main_template.yml
    with:
      modulepath: "contribs/gnomd"
    secrets:
      codecov-token: ${{ secrets.CODECOV_TOKEN }}