name: gno.land

on:
  push:
    branches:
      - master
  workflow_dispatch:
  pull_request: 
    paths:
      - "gno.land/**"
      - ".github/**"

jobs:
  main:
    name: Run Main
    uses: ./.github/workflows/templates/main.yml
    with:
      modulepath: "gno.land"